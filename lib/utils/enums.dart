enum LayoutDirection { ltr, rtl }

enum InputFieldType { loginPassword, phone, cnic, cnicExpiry, email, password, card, search, name, description, menu }

enum InputFieldState { enabled, disabled, readOnly }

enum CountrySelectionType { country, countryCode, currency }

enum TransactionType { send, receive, add }

enum MessageType { mine, other }

enum BookingType { upcoming, inProgress, completed }
